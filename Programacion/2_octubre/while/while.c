#include<stdio.h>
#include<stdlib.h>

/*
 * Autor: David Castejón García 5D DAM
 * Fecha: 
 * Título:
*/
 

 int main (){
	
 	int opcion = 0;// Es necesario, que no obligatorio, inicializar 
		       // la variable porque el primer uso  de la variable es de lectura.	 
	 while(opcion != 4){
	  //Código a ejecutar
	  printf("Introduce una opción: (4 para salir)\n");
	  scanf("%d", &opcion);

			 }
			  
			  return 0;
}
